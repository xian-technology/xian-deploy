---
all:
  children:
    xian_nodes:
      children:
        xian_validators:
          hosts:
            validator-1.example.org:
              ansible_user: deploy
            validator-2.example.org:
              ansible_user: deploy
            validator-3.example.org:
              ansible_user: deploy
        xian_bds_nodes:
          hosts:
            bds-1.example.org:
              ansible_user: deploy
